# Image Toolbox AGENTS

> 本地图片处理小工具：在浏览器内完成批量压缩、改尺寸、转格式和 ZIP 下载，不上传图片，不需要账号和后端。

## 先读文件

- `README.md`：产品定位、功能清单、运行和构建方式。
- `package.json`：Vite/Vue 构建命令。
- `src/`：上传、处理、预览、下载和状态展示逻辑。
- `docs/`：演示素材或补充说明。

## 产品边界

- 核心流程是“选择/拖拽图片 -> 设置格式和尺寸 -> 本地处理 -> 单张下载或 ZIP 下载”。
- 图片必须只在浏览器本地处理，不新增上传、登录、Token、远程存储或服务端转换。
- 工具要服务日常轻量场景：报名照、公众号配图、网页图片瘦身、格式转换。
- 不覆盖用户原图；输出文件名和格式要能区分处理结果。

## 数据边界

- 用户选择的本地图片只存在于浏览器会话和前端内存中。
- 单张处理失败不能中断全部任务。
- 空列表、损坏图片、不支持格式、ZIP 生成失败要有可理解反馈。
- 不把图片内容写入仓库、远程服务或持久化缓存。

## 常用命令

- 开发：`npm run dev`
- 构建：`npm run build`
- 预览：`npm run preview`

## 验收标准

- 多图上传、格式切换、尺寸设置、压缩质量调节可用。
- 单张下载和全部 ZIP 下载可用，文件扩展名与实际格式一致。
- 原图不被覆盖，不发生上传。
- 异常图片或处理失败有提示，其他图片仍可继续处理。
- 改动用户可见功能时同步更新 README 或相关说明。
