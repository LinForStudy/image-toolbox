# 我用 Vibe Coding 做了一个图片工具箱，专治“图片太大传不上去”

今天本来只是想处理几张图。

一张公众号配图太大。

一张截图宽度不统一。

还有几张 PNG，想转成 WebP，放到网页里轻一点。

这些事听起来都不难。

搜一个在线压缩网站，上传，等它处理，再下载回来。

但每次做到这里，我都觉得有点烦。

有的网站广告挤在按钮旁边。

有的网站限制张数。

有的网站处理完文件名变成一串看不懂的字符。

更重要的是，有些图片我其实不太想上传到第三方服务器。

不是多机密。

就是没必要。

于是我冒出一个想法：

能不能做一个很轻的小工具，专门处理这些图片小麻烦？

不用登录。

不用服务器。

不用上传图片。

点开就能用。

后来这个小工具就变成了 Image Toolbox 图片工具箱。

<!-- 配图：Image Toolbox 首页截图，展示压缩、改尺寸、转格式三个模式 -->

## 我只保留了几个功能

一开始我只是想做图片压缩。

但真正用起来才发现，图片处理很少只有“压缩”一个动作。

公众号配图要统一宽度。

网页图片希望体积小一点。

报名系统会限制图片大小。

截图有时候还要转格式。

所以第一版我只保留了几个最常用的功能：

- 批量压缩
- 改尺寸
- PNG / JPEG / WebP 互转
- 单张下载和 ZIP 打包下载

所有处理都在浏览器本地完成。

图片不会上传到服务器。

这个边界一确定，工具就轻了很多。

用户不用登录，也不用理解什么叫云端处理。打开页面，把图片拖进去就行。

## 真正麻烦的是让它顺手

功能跑起来以后，我以为差不多了。

后来自己试了一下，发现体验有点怪。

页面顶部有三个模式：

压缩、改尺寸、转格式。

看起来很清楚。

但点完以后，左边参数变化不明显。

用户就会有一种感觉：

我刚才到底点没点？

这个问题不是 bug。

但很影响使用感。

很多小工具不好用，并不是因为功能少，而是因为它不回应用户。

所以我后来改了一版。

点“压缩”，默认切到 WebP，质量调到 78%，保持原尺寸。

点“改尺寸”，默认切到最长边 1280px，不放大小图。

点“转格式”，保持原尺寸，同时把输出格式区域高亮出来。

这样用户先表达意图，工具再把常用参数摆好。

如果只是临时处理几张图，基本不用想太多。

## 技术栈没什么玄学

这个工具用的是 Vue 3、Vite、TypeScript、Canvas 和 JSZip。

核心逻辑也很直接：

读取图片，用 Canvas 重新绘制，再按用户选择导出格式、尺寸和质量。

最后生成文件，单张下载或者打包成 ZIP。

真正花时间的，反而是一些小地方。

比如 JPEG 要补白底。

比如参数改了以后，旧结果不能继续下载。

比如 ZIP 里文件名重复，不能互相覆盖。

这些都不是大问题。

但它们决定了这个工具是“能跑”，还是“能用”。

## 这次最大的收获

工具跑起来以后，我把它整理到了 GitHub：

https://github.com/LinForStudy/image-toolbox

README 里写清楚它是纯前端、本地处理。

还录了一段操作视频，转成 GIF 放进去。

如果只看结果，这次就是做了一个图片处理小工具。

但我觉得更有价值的是中间那个过程。

Vibe Coding 确实让开发变快了。

但它没有让产品判断变简单。

因为代码变便宜以后，乱加功能的诱惑也变大了。

你更容易一兴奋就做出一堆东西。

最后页面很热闹，但没人真的用。

所以我现在对这类小工具的理解更简单了一点：

先让它点开就能用。

先让它不上传、不登录、不折腾。

先把一个真实的小问题解决顺手。

Image Toolbox 现在还很简单。

但下次再遇到“图片太大传不上去”，至少不用临时打开一堆压缩网站了。

这就够它存在了。

<!-- 配图：GitHub 仓库 README 截图，展示 demo.gif 和本地运行命令 -->
